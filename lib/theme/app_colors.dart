import 'package:flutter/material.dart';

/// Centralized brand colors for the PharmaCare app.
/// Keeping these in one place avoids magic hex codes scattered
/// throughout the widget tree and makes rebranding trivial.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xff0F766E);
  static const Color secondary = Color(0xff16A34A);
  static const Color dark = Color(0xff123047);
  static const Color lightTeal = Color(0xffE6F7F4);
}
